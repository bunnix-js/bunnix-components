# Release 0.11.5

Target package: @bunnix/components@0.11.5. Accumulation branch: release/0.11.5. This implementation prepares package and lockfile version metadata, staged npm publishing, literal PR/issue validation, and tests on every PR target branch. It does not change component APIs.

Merge the implementation PR into release/0.11.5 only when authorized, then review the accumulated release PR into main. After an explicitly authorized main merge, CI stages the package for manual review. Follow ../RELEASING.md for npmjs.com approval with 2FA and publication confirmation. Keep the GitHub release as a draft until confirmation and explicit authorization to publish it.
